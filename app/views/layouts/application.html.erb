<!DOCTYPE html>
<html>
<head>
  <title>Soundbolt</title>
  <%= stylesheet_link_tag    'application', media: 'all' %>
  <%= javascript_include_tag 'application' %>
  <%= csrf_meta_tags %>

  <!-- RAZYNOIR: Use Filepicker -->
  <script type="text/javascript" src="//api.filepicker.io/v2/filepicker.js"></script>
  <script>
    filepicker.setKey("ADEuemHr8QhKL2J02GXF7z");
  </script>

</head>
<body>

  <nav class="navbar navbar-default navbar-fixed-top title-navbar">
    <div class="container title-container">
      <div class="navbar-header">
        <div class="title-elements"><a href="<%= root_url %>" class="elegent-link"><strong>SoundBolt</strong></a></div>
      </div>

      <div class="nav navbar-nav navbar-right">

        <!-- RAZYNOIR: Title elements customized styling. -->
        <div class="title-elements title-small"><a href="#" class="elegent-link">About</a></div>
        <div class="title-elements title-small"><a href="#" class="elegent-link">Support</a></div>

        <% if current_user %>
          <div class="title-elements title-small"><a rel="nofollow" data-method="delete" href="<%= session_url %>" class="elegent-link">Sign Out</a></div>
        <% else %>
          <div class="title-elements title-small"><a href="<%= new_user_url %>" class="elegent-link">Sign Up</a></div>
        <% end %>
      </div>

    </div> <!-- RAZYNOIR: Title container close. -->
  </nav>


  <div class="navbar-buffer"></div>


  <!-- RAZYNOIR-INCOMPLETE: Flash errors styling pending. -->
  <% if flash[:errors] %>
    <h1><%= flash[:errors] %></h1>
  <% end %>

  <!-- RAZYNOIR: The main content goes here. -->
  <div id="soundbolt-content-master">

    <!-- CONTENT-START -->

    <%= yield %>

    <!-- CONTENT-END -->

  </div>

  <!-- RAZYNOIR: The focus overlay goes here. -->
  <div id="soundbolt-focus-master">
  </div>


  <% if current_user %>
    <div class="navbar-foot-buffer"></div>

    <!-- RAZYNOIR: The bottom playbar goes here. -->
    <div id="soundbolt-player-master">
    </div>
  <% end %>

  <script>
    <% if current_user %>
      var currentUserId = <%= current_user.id %>;
    <% end %>
  </script>

</body>
</html>
